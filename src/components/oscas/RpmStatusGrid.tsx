import React from "react";
import RpmStatus from "./RpmStatus";


// @ts-ignore
function RpmStatusGrid({children} ) {
    return (
        <div className={'grid'}>{children}</div>
    );
}

export default RpmStatusGrid;
