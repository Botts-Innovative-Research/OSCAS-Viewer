import React from "react";
import LaneCamera from "./LaneCamera";


// @ts-ignore
function LaneCamera({children} ) {
    return (
        <div className={'grid'}>{children}</div>
    );
}

export default LaneCameraGrid;